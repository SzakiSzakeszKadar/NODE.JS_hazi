<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ügyfél Részletes Adatai</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f5f5f5;
    }
    .container {
      max-width: 800px;
      margin: 0 auto;
      background-color: white;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1 {
      color: #333;
      text-align: center;
      margin-bottom: 30px;
    }
    .customer-details {
      margin-bottom: 30px;
    }
    .detail-group {
      margin-bottom: 20px;
      padding: 15px;
      background-color: #f8f9fa;
      border-radius: 5px;
    }
    .detail-label {
      font-weight: bold;
      color: #666;
      margin-bottom: 5px;
    }
    .detail-value {
      color: #333;
      font-size: 1.1em;
    }
    .status, .membership {
      display: inline-block;
      padding: 6px 12px;
      border-radius: 4px;
      font-weight: bold;
    }
    .active {
      background-color: #4CAF50;
      color: white;
    }
    .inactive {
      background-color: #f44336;
      color: white;
    }
    .Bronze {
      background-color: #cd7f32;
      color: white;
    }
    .Silver {
      background-color: #c0c0c0;
      color: white;
    }
    .Gold {
      background-color: #ffd700;
      color: black;
    }
    .button-group {
      display: flex;
      gap: 10px;
      justify-content: center;
      margin-top: 20px;
    }
    .button {
      padding: 10px 20px;
      border-radius: 4px;
      text-decoration: none;
      color: white;
      font-weight: bold;
      text-align: center;
    }
    .edit {
      background-color: #4CAF50;
    }
    .delete {
      background-color: #f44336;
    }
    .back {
      background-color: #95a5a6;
    }
    .button:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Ügyfél Részletes Adatai</h1>
    <div class="customer-details">
      <div class="detail-group">
        <div class="detail-label">Név:</div>
        <div class="detail-value"><%= customer.name %></div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Email:</div>
        <div class="detail-value"><%= customer.email %></div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Telefonszám:</div>
        <div class="detail-value"><%= customer.phone %></div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Cím:</div>
        <div class="detail-value"><%= customer.address %></div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Tagság típusa:</div>
        <div class="detail-value">
          <span class="membership <%= customer.membershipType %>">
            <%= customer.membershipType %>
          </span>
        </div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Csatlakozás dátuma:</div>
        <div class="detail-value">
          <%= customer.joinDate.toLocaleDateString('hu-HU', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
          }) %>
        </div>
      </div>
      <div class="detail-group">
        <div class="detail-label">Státusz:</div>
        <div class="detail-value">
          <span class="status <%= customer.active ? 'active' : 'inactive' %>">
            <%= customer.active ? 'Aktív' : 'Inaktív' %>
          </span>
        </div>
      </div>
    </div>
    <div class="button-group">
      <a href="/customer/edit/<%= customer._id %>" class="button edit">Szerkesztés</a>
      <a href="/customer/delete/<%= customer._id %>" class="button delete" onclick="return confirm('Biztosan törölni szeretnéd ezt az ügyfelet?')">Törlés</a>
      <a href="/customer" class="button back">Vissza</a>
    </div>
  </div>
</body>
</html>
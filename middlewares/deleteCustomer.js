/**
 * torlok egy customer-t az adatbazisbol es atiranyitja a usert a /vhs -re
 * @param objRepo
 * @returns {function(*,*,*): *}
 */
module.exports = (objRepo) => {
    return (req, res, next)=> {
        return next();
    }
}